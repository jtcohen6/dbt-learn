name: 'dbt_learn'
version: '1.0'

profile: 'dbt_learn'

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"

models:
  dbt_learn:
    marts:
      materialized: table
