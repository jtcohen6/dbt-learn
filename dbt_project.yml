name: jaffle_shop

profile: "garage-bigquery"

version: '1.0'

require-dbt-version: "0.18.1"
config-version: 2

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"

seeds:
  quote_columns: false

quoting:
  database: false
  identifier: false
  schema: false

models:
  transient: true
